spec file for building RPM package on Fedora

requires:
    rpm-build

build:
    rpmbuild --undefine=_disable_source_fetch -ba mtn.spec


references:
  https://fedoraproject.org/wiki/How_to_create_an_RPM_package
